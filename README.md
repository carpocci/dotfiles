# Andrea Carpino's dotfiles

## Setup

Clone this repository in your home directory:
```git clone https://github.com/carpocci/dotfiles.git ~/.dotfiles```

Install the dependencies and set up the environment:
```~/.dotfiles/install.sh```